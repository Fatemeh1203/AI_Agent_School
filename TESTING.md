# چک‌لیست تست دستی

پیش از شروع: هر ۴ workflow را Active کنید، حداقل یک ردیف در `students` و شش ردیف در
`staff` (شامل یک `مدیر`) داشته باشید، و صفحه‌ی وب را روی `WEBHOOK_URL`/`API_KEY` درست
تنظیم کرده باشید.

## ۱. منوی اصلی و FAQ

- [ ] بازکردن `web/index.html` منوی اصلی را با نام مدرسه در هدر نشان می‌دهد.
- [ ] ارسال `/start` در تلگرام همان دکمه‌های منو را نشان می‌دهد (به تعداد ردیف‌های
      `faq` با `menu_path` پر، به‌علاوه «سؤال دیگری دارم» و «ورود اولیا»).
- [ ] یک ردیف جدید در `faq` با `menu_path` پر اضافه کنید (بدون هیچ تغییری در workflow) —
      دفعه‌ی بعد که منو باز می‌شود، دکمه‌ی جدید باید ظاهر شود.
- [ ] کلیک روی یکی از دکمه‌های FAQ پاسخ درست از ستون `answer` را نشان می‌دهد.
- [ ] تایپ یک سؤال آزاد که با `keywords` یکی از ردیف‌های faq همخوانی دارد (مثلاً «شهریه
      چقدره») پاسخ درست را برمی‌گرداند.
- [ ] تایپ سؤالی کاملاً بی‌ربط یک کد پیگیری برمی‌گرداند و یک ردیف در `unanswered` و یک
      ردیف در `tickets` (با `category='سوال_آزاد'`) ثبت می‌شود.

## ۲. ثبت‌نام مرحله‌ای

- [ ] «شروع ثبت‌نام» → انتخاب پایه → نام دانش‌آموز → نام ولی → موبایل → توضیح (رد یا وارد
      کردن) → خلاصه → تأیید. یک ردیف در `leads` و یک ردیف در `registrations` با
      `completed_at` پر ساخته می‌شود.
- [ ] وسط فرآیند (مثلاً بعد از وارد کردن نام دانش‌آموز) صفحه را ببندید/برنامه‌ی تلگرام را
      عوض کنید، سپس دوباره پیام بدهید — باید دقیقاً از همان مرحله (نام ولی) ادامه دهد، نه
      از اول.
- [ ] شماره‌ی موبایل نامعتبر (مثلاً `0912345`) رد می‌شود و دوباره همان سؤال پرسیده می‌شود؛
      شماره با ارقام فارسی (`۰۹۱۲۳۴۵۶۷۸۹`) باید پذیرفته شود.
- [ ] بعد از تأیید نهایی، اگر `staff` یک ردیف با `category='مدیر'` و `telegram_chat_id`
      معتبر داشته باشد، همان لحظه یک پیام اعلان در تلگرام دفتر دریافت می‌شود.

## ۳. اولیای احراز هویت‌شده

- [ ] «ورود اولیا» → کد رهگیری اشتباه سه بار پشت‌سرهم → پیام قفل یک‌ساعته نشان داده
      می‌شود.
- [ ] تلاش چهارم (در حالت قفل) باز هم پیام قفل را نشان می‌دهد، نه سؤال کد را دوباره.
- [ ] با `unique_code` درست، وارد منوی دسته‌بندی (مالی/آموزشی/انضباطی/مرخصی/سایر) می‌شود.
- [ ] ثبت یک درخواست در یک دسته یک ردیف در `tickets` با `student_id` پر و `assignee_id`
      برابر یکی از `staff` هم‌دسته می‌سازد (اگر چنین کارمندی وجود داشته باشد).
- [ ] بررسی کنید که بدون `verified_at` (قبل از وارد کردن کد درست) هیچ نامی از دانش‌آموز در
      پاسخ‌ها ظاهر نمی‌شود.

## ۴. برابری خروجی دو کانال (معیار پذیرش اصلی)

- [ ] برای یک کاربر جدید در هر دو کانال، ارسال ورودی `{type:'choice', value:'show_menu'}`
      (وب) و `/start` (تلگرام) باید همان متن و همان گزینه‌ها را برگرداند.
- [ ] تایپ عین همان سؤال آزاد در وب و تلگرام باید همان پاسخ (یا همان کد پیگیری تیکت با
      همان قالب) را برگرداند.

## ۵. حالت‌های خطا

- [ ] Webhook وب را بدون هدر `X-API-Key` صدا بزنید (مثلاً با curl) — باید ۴۰۱/۴۰۳ برگرداند،
      نه پردازش عادی.
- [ ] بیش از ۲۰ درخواست در یک دقیقه از یک IP به Webhook وب بزنید — از یک جایی به بعد باید
      پیام «تعداد درخواست‌های زیاد» با کد ۴۲۹ برگردد.
- [ ] PostgreSQL را موقتاً متوقف کنید و یک پیام به CORE بفرستید — کاربر باید پیام فارسی
      عمومی + تلفن دفتر ببیند، نه خطای خام یا Stack Trace.
- [ ] یک درخواست وب بدون `sessionId` یا با `input` ناقص بفرستید — باید خطای ۴۰۰ با پیام
      فارسی برگردد.

## ۶. SLA و گزارش هفتگی

- [ ] یک ردیف تیکت با `created_at` دستی (`now() - interval '4 hours 5 minutes'`) و
      `first_reply_at IS NULL` بسازید؛ در اجرای بعدی WF-SLA (یا اجرای دستی نود)، مسئول
      هم‌دسته باید پیام یادآوری بگیرد.
- [ ] همین کار را برای `created_at` معادل ۲۴ ساعت و ۵ دقیقه پیش تکرار کنید؛ مدیر باید
      پیام هشدار بگیرد.
- [ ] workflow گزارش هفتگی را دستی (Execute Workflow از نود «زمان‌بند گزارش هفتگی») اجرا
      کنید؛ پیام دریافتی مدیر باید شامل تعداد به‌تفکیک دسته، میانگین زمان پاسخ، تعداد
      تیکت باز و ۵ سؤال پرتکرار (با تاریخ شمسی در خط اول) باشد.

## ۷. پنل محتوا (NocoDB)

- [ ] با کاربر `hadaf_nocodb_user` وارد NocoDB شوید و مطمئن شوید فقط دو جدول `faq` و
      `settings` قابل مشاهده‌اند.
- [ ] یک مقدار در `settings` (مثلاً `office_phone`) را عوض کنید و بدون هیچ راه‌اندازی مجدد
      workflow، در تلگرام/وب سؤال بپرسید که آن مقدار را نشان می‌دهد — باید فوراً مقدار
      جدید را ببینید.
